[build]
  publish = "example/storybook-static"
  command = "npm install && npm run build && npm --prefix example install && npm --prefix example run build-storybook"
[build.environment]
  NODE_VERSION = "22"
